# Prevents ESLint warnings from being treated as build-breaking errors on CI/Vercel.
# (Standard setting for Create React App deployments — these are pre-existing
# accessibility lint warnings on decorative placeholder links, not functional bugs.)
CI=false
